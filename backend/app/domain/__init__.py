"""Core Domain.

Business rules and the ports (interfaces) the domain needs from the outside
world. Nothing under app/domain/ imports FastAPI, a subprocess, an HTTP
client, or any adapter — see docs/architecture/README.md.
"""
