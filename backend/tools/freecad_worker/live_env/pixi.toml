[workspace]
name = "craftsman-live-env"
description = "xpra only - streams the real FreeCAD GUI (Craftsman live mode) into the browser without needing sudo/apt. See docs/CRAFTSMAN_AGENT.md."
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
xpra = "*"
xorg-xvfb-server = "*"
xkeyboard-config = "*"
